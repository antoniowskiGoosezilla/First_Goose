using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

namespace AntoNamespace{
    [RequireComponent(typeof(AIStatsHandler))]
    [RequireComponent(typeof(AIStateHandler))]
    [RequireComponent(typeof(AILocomotionHandler))]
    [RequireComponent(typeof(AIAttackHandler))]
    [RequireComponent(typeof(NavMeshAgent))]
    public class AICore : MonoBehaviour
    {
        
    }
}
